import React from "react";
import Headers from "../intro/headers";
import Animation from "../intro/animation";

const Intro = () => {
  return (
    <div>
      <Headers />
      <Animation />
    </div>
  );
};

export default Intro;
